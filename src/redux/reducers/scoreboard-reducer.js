const initialState = {
    scoreboard: {
        currentValue: 0,
        result: null,
    }
}

const scoreboardReducer = (state=initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default scoreboardReducer